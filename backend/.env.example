# Platform EigenCompute credentials
# Get your private key via: ecloud auth login, then export from keyring
# Or use the wallet private key shown by: ecloud auth whoami
EIGENCOMPUTE_PRIVATE_KEY=0x_your_platform_private_key
EIGENCOMPUTE_ENVIRONMENT=sepolia

# Pre-built agent Docker image (must be pushed to Docker Hub)
# Build: docker build --platform linux/amd64 -t YOUR_USERNAME/skillsseal-agent:latest ./agent
# Push:  docker push YOUR_USERNAME/skillsseal-agent:latest
AGENT_IMAGE_REF=YOUR_USERNAME/skillsseal-agent:latest

# CORS origin for frontend
FRONTEND_URL=http://localhost:3000

# Server port
PORT=3002

# Session token signing secret (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# If not set, a random secret is generated on startup (tokens invalidated on restart)
TOKEN_SECRET=
